# Valid: universe selections in both forms (RFC-0004) — the scalar
# shorthand (no attribution) and the object form carrying selected_by /
# reviewed_by in both the actor-id and {actor, role} shapes.
id: baseline_attributed
description: Baseline configuration with attributed selections.
decisions:
  scaling:
    decision_id: scaling
    option_id: standard_after_split
    selected_by: {actor: jane, role: methodology}
  model:
    decision_id: model
    option_id: random_forest
    selected_by: {actor: assistant, role: methodology}
    reviewed_by: {actor: jane, role: validation}
  outlier_handling: keep_all
